# hcli.sec scope file — one entry per line.
# Wildcards: *.example.com includes all subdomains.
# Lines starting with '#' are comments and are ignored.
# Pass this file via:  hcli.sec hunt --target example.com --scope scope_example.txt

example.com
*.example.com
api.example.com
staging.example.com
